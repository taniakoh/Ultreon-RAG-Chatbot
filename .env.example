# --- API KEYS ---
# Provided rate-limited key from UltreonAI
OPENROUTER_API_KEY=your_key_here

# --- LLM & EMBEDDINGS ---
# LLM model (via OpenRouter)
LLM_MODEL=anthropic/claude-3.5-sonnet

# Embedding model (HuggingFace model name)
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
NEXT_PUBLIC_API_URL=http://localhost:8000
# --- RAG PIPELINE TUNING ---
# Size of text chunks (in tokens)
CHUNK_SIZE=512
# Overlap between consecutive chunks
CHUNK_OVERLAP=64
# Number of snippets to retrieve per query
SIMILARITY_TOP_K=4

# --- INFRASTRUCTURE (GCP/DOCKER) ---
# Project ID for Google Cloud
GCP_PROJECT_ID=your-project-id
# Bucket name for persisting ChromaDB in production
GCS_BUCKET_NAME=your-gcs-bucket-name

# --- NETWORKING ---
# Ports for local development
BACKEND_PORT=8000
FRONTEND_PORT=3000
DOMAIN_NAME=localhost